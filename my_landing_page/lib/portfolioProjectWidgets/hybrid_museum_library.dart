import 'package:flutter/material.dart';

class HybridMLibrary extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.white,
      child: Text('HybridMuseumLibrary',
          style: TextStyle(
            color: Colors.black,
          )),
    );
  }
}
