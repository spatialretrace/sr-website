import 'package:flutter/material.dart';

class MSchool extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.white,
      child: Text('MacchegaunSchool',
          style: TextStyle(
            color: Colors.black,
          )),
    );
  }
}
